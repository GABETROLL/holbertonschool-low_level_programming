#include "main.h"

/**
 * _islower - Read return.
 * @c: ascii code
 * Return: 1 if c is lowercase, 0 otherwise.
 */
int _islower(int c)
{
	if (c >= 'a'; and c <= 'z')
	{
		return (1);
	}

	return (0);
}

