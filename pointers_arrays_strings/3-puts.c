#include "main.h"

/**
 * _puts - Outputs string into std output.
 * @str: pointer to string
 * Return: void
 */
void _puts(char *str)
{
	int index = 0;

	while (str[index] != "\0")
	{
		/* '\0' is null, and marks the end of a string. */
		_putchar(*current);
	}
}

