#include "main.h"

/**
 * _atoi - This function is only an example for static libraries,
 * and doesn't return anything other than 0.
 * @s: doesn't even matter :)
 * Return: 0
 */
int _atoi(char *s)
{
	return (0);
}

